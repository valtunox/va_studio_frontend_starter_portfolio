<<<<<<< HEAD
# For VM deploy only: frontend uses pre-pulled image (no build). Nginx builds from ./nginx.
services:
  frontend:
    container_name: studioapp-frontend-portfolio
    image: ${FRONTEND_IMAGE}
    restart: unless-stopped
    ports:
      - "3051:3051"
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 512M
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3051/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - studioapp-net-portfolio

  nginx:
    container_name: studioapp-nginx-portfolio
    image: ${NGINX_IMAGE:-studioapp-nginx-portfolio:latest}
    restart: unless-stopped
    build:
      context: ./nginx
      dockerfile: Dockerfile
      args:
        APP_PORT: 3051
        HTTP_PORT: 80
    ports:
      - "80:80"
      - "443:443"
    environment:
      - app_name=studioapp-frontend-portfolio
      - app_port=3051
    volumes:
      - ${SSL_CERTS_PATH:-./certs}:/etc/nginx/ssl:ro
    depends_on:
      frontend:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:80/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    networks:
      - studioapp-net-portfolio

networks:
  studioapp-net-portfolio:
    driver: bridge
=======
# For VM deploy only: frontend uses pre-pulled image (no build). Nginx builds from ./nginx.
services:
  frontend:
    container_name: studioapp-frontend-portfolio
    image: ${FRONTEND_IMAGE}
    restart: unless-stopped
    ports:
      - "3051:3051"
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 512M
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3051/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - studioapp-net-portfolio

  nginx:
    container_name: studioapp-nginx-portfolio
    image: ${NGINX_IMAGE:-studioapp-nginx-portfolio:latest}
    restart: unless-stopped
    build:
      context: ./nginx
      dockerfile: Dockerfile
      args:
        APP_PORT: 3051
        HTTP_PORT: 80
    ports:
      - "80:80"
      - "443:443"
    environment:
      - app_name=studioapp-frontend-portfolio
      - app_port=3051
    volumes:
      - ${SSL_CERTS_PATH:-./certs}:/etc/nginx/ssl:ro
    depends_on:
      frontend:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:80/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    networks:
      - studioapp-net-portfolio

networks:
  studioapp-net-portfolio:
    driver: bridge
>>>>>>> 22b0267a21511be5230e2e8a7fbb8f3f2d788d52
